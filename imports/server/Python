innerhalb des replytics Ordner: meteor npm install --save python-shell

python -m pip install pymongo

python -m pip install -U textblob-de
python -m textblob.download_corpora
python -m pip install regex

